# fitmeal

App de planificación de comidas para pareja (PWA, React + Vite + TypeScript + Tailwind + Supabase).

Para configurar el proyecto desde cero (Supabase, claves, primer arranque), sigue [SETUP.md](SETUP.md).

```
npm install
npm run dev
```
